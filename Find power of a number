
#include<stdio.h>
#include<math.h>
int power(int base,int exp)
{
    int result;
    result=pow(base,exp);
    return result;
}
int main()
{
    int a,b,c;
    printf("enter the value of a,b");
    scanf("%d%d",&a,&b);
    c=power(a,b);
    printf("power of a number %d",c);
}
