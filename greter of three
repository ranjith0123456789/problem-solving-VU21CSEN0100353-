#include<stdio.h>
int main(int argc, char const *argv[])
{
    int a,b;
    printf("please enter the integer:");
    scanf("%d%d",&a,&b);
    if (a<=b)
    if(a<b)
        printf("%d<%d\n",a,b);
    else
        printf("%d==%d\n",a,b);
    else
        printf("%d>%d\n",a,b);
    return 0;
}
