
#include<stdio.h>
int max(int a,int b)
{
   if(a>b)
   {
       return a;
   }
   else
   {
      return b; 
   }
}
   
int main()
{
    int a,b,c;
    printf("enter the value of a,b");
    scanf("%d%d",&a,&b);
    c=max(a,b);
    printf("max of a number %d",c);
}
